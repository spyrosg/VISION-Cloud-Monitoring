/* global define, describe, it, expect */

define(['util'], function(util) {
    'use strict';

    var Observer = util.Observer,
        Observable = util.Observable;

    describe("implementing the Observer Pattern", function() {
        it("an observer should be able to register to an observable", function() {
        });

        it("another foo", function() {
        });
    });
});
